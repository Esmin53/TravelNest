import { ReactNode } from "react"


interface AditionalOptionProps {
    icon: ReactNode,
    title: string
}

const AdditionalOption = ({
    icon,
    title
}: AditionalOptionProps) => {

    return <div className="w-full">

    </div>
}

export default AdditionalOption